import 'package:flutter/material.dart';

class FruitWidget extends StatelessWidget {
  const FruitWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("404 Error : TU PUES MAIS DANS UN FRUIT"),
    );
  }
}
