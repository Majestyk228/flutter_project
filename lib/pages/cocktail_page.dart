import 'package:flutter/material.dart';

class CocktailWidget extends StatelessWidget {
  const CocktailWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("404 Error : TU PUES MAIS DANS UN COCKTAIL"),
    );
  }
}
